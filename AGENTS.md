# Wolfram Physics Explorer

This checkout belongs to the parent Wolfram Physics workspace. Its selected knowledge vault is `../wolfram-physics-vault/`. Read that vault's schema, root index, and `wolfram/index.md`; for physics work read `wolfram/foundations.md` and the relevant primary sources.

Strict fidelity to documented Wolfram model rules is the acceptance standard. Preserve ordered relations, multiplicity, repeated variables, fresh atoms, and valid event consumption. Do not substitute graph animation or undocumented growth heuristics for rewriting. Distinguish simulation state from rendering data and distinguish a bounded execution from a claim about fundamental physics.

Inspect current files and Git before trusting stored knowledge. Repowise is optional; this local index supports structural retrieval with a mock embedder. It does not establish semantic similarity or engine correctness. Existing uncommitted work must be preserved. Follow the active session's approval rules for Git delivery and full or paid runs.
